const state = () => ({
    itemtabs: [
        'users',
        'roles',
        'permissions',
    ],
})

const mutations = {
}

const actions = {
}

export default {
    namespaced: true,
    state,
    actions,
    mutations
}

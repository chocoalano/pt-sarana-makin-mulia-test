const state = () => ({
    itemtabs: [
        'master',
        'permintaan',
    ],
})

const mutations = {
}

const actions = {
}

export default {
    namespaced: true,
    state,
    actions,
    mutations
}
